# Define a list of names
friends  = ["ashu", "asif", "fahim", "omor", "mizan"]
len(friends)
# Iterate through each name in the list
for name in range(5):
    print(name) #it will print the index
print(len(friends), "is the number of elements") # it will print how much element is stored
